# 目的
- 根據菜單內容（`menu.jpg`），設計一個方便班級學生點飲料的網站 
- 總共兩個網頁，主網頁點餐，另外一個網頁查看目前點餐狀況
- 網站設計風格需與菜單同樣色系，簡單、明亮

# 排版
- 網頁排版由上而下以 `列` 為單位，每列中如果有多個區塊，以 `|` 符號隔開，例如 `選單 | 詳細內容`，表示該列有兩個區塊
- 點餐網頁需要 2 列，各列中的區塊如下
1. 菜單
2. 表單 | 價格

# 區塊內容說明
## 菜單
- 顯示菜單，檔案為 `menu.jpg`
- 等比例縮放，不可裁切

## 表單
- 根據 `menu.jpg` 內容使用 `友善的方式` 讓使用者選擇餐點，
- 菜單中的所有餐點項目都必須出現在表單中，不可遺漏
- 表單中加上 `輸入姓名` 作為餐點識別
- 表單送出後的資料儲存於 Google Sheet，檔名為 `2026手搖飲點餐記錄`。第一列為標題列，需填上標題名稱，並且在該 Google Sheet 中部署 Apps Script

## 價格
- 根據表單內容即時計算點選的餐點價格，表單送出時 **必須** 將此價格一起送出

# 驗證與測試
- 使用瀏覽器確認各語法是否正確
- 檢查各區塊的排版是否嚴格遵守排版要求
- 實際送出兩筆訂單資料，並且至 Google Sheet 檢查是否存入
- 有錯自行修正

# 部署
- 檢查本機是否安裝 gh cli，若未安裝，協助使用者下載並安裝
- 建立 github repo，名稱為 `2026teashop`
- 將網站推送到 github 並且啟用 pages 功能
- 不要推送 spec 資料夾

# 其他注意事項
將 pages 網址寫入 README.md